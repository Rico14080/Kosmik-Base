# V1.14 backend restore

The exact V1.14 source package contains the complete backend at `backend/server.py` (58 KB) plus its test and support files.

The repository upload omitted that source file. To preserve fidelity, do not recreate it from memory and do not use a simplified replacement.

Manual action required:
1. Open the original `PROGETTO_COSMIK_CIRCLES_V1.14_GALLERY_ARCHIVE.zip` locally.
2. Enter `backend/`.
3. Upload the original `server.py` to this repository on branch `development`.
4. Also upload the original `backend/tests/smoke_test.py`, `backend/PRODUCTION_SETUP.md`, `backend/start_server.bat`, `backend/backup.py`, `backend/seed_gallery_demo.py`, and `backend/seed_temp_image_slots.py` where needed.
5. Do not upload `backend/data/`, `backend/uploads/`, `.env`, or `__pycache__/`.

After the exact backend is present, CI and the full end-to-end audit can run against the real V1.14 implementation.