# Kosmik Circles V1.13 — Managed Image Slot Replacement

- Former CSS artwork behind managed image areas has been removed when the slot is intended for custom imagery.
- Home hero now uses a real `<img>` layer; orbit/planet CSS artwork is disabled and is no longer used as a fallback.
- Shop product cards are image areas only; legacy pseudo-element artwork is disabled.
- Us / Studio keeps the orange box as the empty-slot area, but removes the CSS orbital ring; a custom image replaces the box.
- All custom image layers use normal object rendering, not blend modes.
- Added `TEMP_IMAGE_SLOT_TEST.webp` and `backend/seed_temp_image_slots.py` for isolated local verification.
