const STORAGE_KEY = 'kosmik-circles-content';
const CART_KEY = 'kosmik-circles-cart';
const API_BASE = (location.protocol === 'http:' || location.protocol === 'https:') ? `${location.origin}/api` : 'http://127.0.0.1:8080/api';
const defaultContent = {
  home: { eyebrow:'Independent objects / Est. 2024', titleLineOne:'Made for', titleLineTwo:'elsewhere.', intro:'Small-batch goods for people who keep looking up. Wearable signals, useful artifacts, and a little cosmic noise.', cta:'Enter the orbit', heroImage:'https://images.unsplash.com/photo-1534791547706-5c292f749e1b?auto=format&fit=crop&w=1200&q=85', heroAlt:'Orange light cutting through a dark concert atmosphere', signalText:'new objects for old souls', orbitLabelTop:'38° 11′ 52″ N', orbitLabelBottom:'signal / 001', heroIndex:'01 / 04', sectionNumber:'[ 001 ]', signalStripLabel:'Currently transmitting', introEyebrow:'The short version', introTitleLineOne:'Good things can', introTitleLineTwo:'still feel unknown.', introDescription:'Kosmik Circles is a design studio making limited-run pieces with a point of view. We work slowly, source carefully, and leave enough room for the weirdness to get in.' },
  pages: { shop:{eyebrow:'Available now / Dispatching worldwide',titleLineOne:'Objects with',titleLineTwo:'an orbit.',note:'Four small-batch pieces. No restocks promised.'}, gallery:{eyebrow:'Visual archive / Night folders',titleLineOne:'Enter the',titleLineTwo:'archive.',note:'Open a night. Download the signal.'}, live:{eyebrow:'Transmission schedule / 2026',titleLineOne:'Come',titleLineTwo:'through.',note:'Night flights, deep rooms, high frequencies.'}, contact:{eyebrow:'Open frequency / hello@kosmikcircles.com',titleLineOne:'Send a',titleLineTwo:'signal.',note:''}, us:{eyebrow:'A very small studio',titleLineOne:'We make',titleLineTwo:'signals.',note:''}, cart:{eyebrow:'Your selected objects',titleLineOne:'Enter the',titleLineTwo:'cart.',note:'Leave your email and send the order signal.'} },
  ticker:{text:'NEW TRANSMISSION SOON ✳ KOSMIK CIRCLES / SMALL BATCH / LIVE AUDIOVISUAL SIGNALS',speed:28,fontSize:11}, matrix:{speed:0.45}, gallery:Array.from({length:5},(_,i)=>({image:'',alt:'',caption:`${String(i+1).padStart(3,'0')} / Empty frequency`})),
  contact:{email:'hello@kosmikcircles.com',instagram:'Instagram',instagramUrl:'',sectionNumber:'[ 003 ]',description:'Kosmik Circles offers live electronic music, audiovisual performances, DJ sets, and sound direction for clubs, festivals, brands, and private spaces. Tell us what you are building and we will shape the frequency with you.',serviceTitleOne:'We create',serviceTitleTwo:'signals.',formIntro:'Start with an email. We answer within 2-3 Earth days.'}, live:[{date:'18.10.24',isoDate:'2024-10-18',location:'Milano, IT',venue:'Magazzini Generali / 23:00',signal:'Circles / 01',detail:'Full live set',action:'Tickets',ticketUrl:'',past:true},{date:'02.11.24',isoDate:'2024-11-02',location:'Berlin, DE',venue:'Ritter Butzke / 00:30',signal:'Night Channel',detail:'2 hour live set',action:'Tickets',ticketUrl:'',past:true},{date:'24.01.25',isoDate:'2025-01-24',location:'Lisboa, PT',venue:'Lux Frágil / 01:00',signal:'Outer Room',detail:'Live + visual show',action:'Tickets',ticketUrl:'',past:true},{date:'31.08.24',isoDate:'2024-08-31',location:'Paris, FR',venue:'La Machine / 23:30',signal:'Soft Landing',detail:'Archive recording',action:'Archive',ticketUrl:'',past:true}], shop:[],
  us:{sectionNumber:'[ 002 ]',statement:'There is a lot of noise out there. We like the kind that means something.',description:'Kosmik Circles started with two friends, a stack of old astronomy books, and a shared belief that everyday things deserve a little mystery. We make products in limited runs, with honest materials, and an unreasonable amount of attention.',photo:'https://images.unsplash.com/photo-1514525253161-7a46d19cd819?auto=format&fit=crop&w=1000&q=85',photoAlt:'Kosmik Circles studio',studioLabel:'KC',soundEyebrow:'The sound / Live frequency',soundTitleLineOne:'Built for',soundTitleLineTwo:'the room.',soundDescription:'Our sound moves between hypnotic low-end, psychedelic textures, and long-form tension. These images are fragments from the places where the signal becomes physical.',soundImages:[{image:'https://images.unsplash.com/photo-1524365252-6f0b8f4e8f7a?auto=format&fit=crop&w=1000&q=85',alt:'DJ performing under red stage lights',caption:'01 / Low light'},{image:'/WhatsApp Image 2026-08-11 at 00.52.13 (13).webp',alt:'Abstract concert lights in a dark room',caption:'02 / Deep signal'},{image:'https://images.unsplash.com/photo-1514525253161-7a46d19cd819?auto=format&fit=crop&w=1000&q=85',alt:'Crowd moving beneath concert lights',caption:'03 / Shared frequency'}],valuesEyebrow:'Our coordinates',valuesTitleLineOne:'Slow things.',valuesTitleLineTwo:'Strange things.',valuesTitleLineThree:'Good things.',valuesDescription:'Designed in Milan. Made with people we know. Packed by hand. Every object has a trace of where it came from.'},
  visuals:{liveBackgroundImage:'https://images.unsplash.com/photo-1514525253161-7a46d19cd819?auto=format&fit=crop&w=1200&q=85'},
  siteText:{skipToContent:'Skip to content',liveUpdates:'Live updates',nav:{home:'Home',shop:'Shop',us:'Us',live:'Live',gallery:'Gallery',contact:'Contact',bag:'Bag',cart:'Cart'},home:{currentlyTransmitting:'Currently transmitting'},shop:{addToCart:'Add to cart'},gallery:{emptyImage:'Open a night archive'},live:{date:'Date',location:'Location',signal:'Signal',noTicket:'Tickets'},contact:{name:'Your name',email:'Your email',message:'Your message',submit:'Transmit'},cart:{yourName:'Your name',yourEmail:'Your email',phone:'Phone',address:'Address',city:'City',postcode:'Postcode',country:'Country',sendOrder:'Send order',remove:'Remove',empty:'Your cart is orbiting empty.',sending:'Sending order…',quantity:'quantity',total:'Total'},footer:{tagline:'Made on Earth, for now',shopCta:'Say hello',homeCta:'Say hello',galleryCta:'Send a signal',liveCta:'Book a transmission',usCta:'Say hello',contactCta:'Browse objects',cartCta:'Back to shop'}}
};
let remoteContent = null;
function deepMerge(base,saved){if(Array.isArray(base))return Array.isArray(saved)?saved:JSON.parse(JSON.stringify(base));if(base&&typeof base==='object'){const out={...base};if(saved&&typeof saved==='object'&&!Array.isArray(saved))for(const k of Object.keys(saved))out[k]=k in out?deepMerge(out[k],saved[k]):saved[k];return out;}return saved===undefined?base:saved;}
function getLocalContent(){try{return JSON.parse(localStorage.getItem(STORAGE_KEY)||'{}')}catch{return {}}}
function mergeContent(saved){return deepMerge(defaultContent,saved||{})}
function getContent(){return remoteContent||mergeContent(getLocalContent())}
async function fetchContent(){try{const r=await fetch(`${API_BASE}/content`,{headers:{Accept:'application/json'},cache:'no-store'});if(!r.ok)throw new Error('API unavailable');const d=await r.json();remoteContent=mergeContent(d.content||{});return remoteContent}catch{return mergeContent(getLocalContent())}}
function isSafeUrl(v='',opts={}){const raw=String(v).trim();if(!raw)return false;if(opts.allowDataImage&&/^data:image\/(?:png|jpe?g|webp|gif);base64,/i.test(raw))return true;if(raw.startsWith('/')||raw.startsWith('./')||raw.startsWith('../')||raw.startsWith('#'))return true;try{const u=new URL(raw,location.href);return u.protocol==='https:'||(opts.allowMailto&&u.protocol==='mailto:')}catch{return false}}
function safeExternalUrl(v=''){return isSafeUrl(v)?String(v).trim():''}
function safeImageUrl(v=''){return isSafeUrl(v,{allowDataImage:true})?String(v).trim():''}
function cssUrl(v=''){const s=String(v||'');return `url(${JSON.stringify(s)})`}
function parsePrice(v=''){const raw=String(v).trim().replace(/[^0-9,.]/g,'');if(!raw)return 0;const n=raw.includes(',')&&raw.includes('.')?(raw.lastIndexOf(',')>raw.lastIndexOf('.')?raw.replace(/\./g,'').replace(',','.'):raw.replace(/,/g,'')):raw.replace(',','.');const p=Number.parseFloat(n);return Number.isFinite(p)?p:0}
function normalizeQuantity(v){const n=Number.parseInt(v,10);return Number.isFinite(n)?Math.min(99,Math.max(1,n)):1}
function normalizeCart(cart){return Array.isArray(cart)?cart.map(i=>({name:String(i?.name||'').trim().slice(0,160),price:String(i?.price||'').trim().slice(0,40),image:safeImageUrl(i?.image||''),quantity:normalizeQuantity(i?.quantity)})).filter(i=>i.name):[]}
function getCart(){try{return normalizeCart(JSON.parse(localStorage.getItem(CART_KEY)||'[]'))}catch{return []}}
function saveCart(c){localStorage.setItem(CART_KEY,JSON.stringify(normalizeCart(c)))}
function escapeHtml(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c))}
function updateCopyrightYear(){const y=String(new Date().getFullYear());document.querySelectorAll('[data-current-year]').forEach(e=>e.textContent=y)}
function updateBagCount(){const count=getCart().reduce((t,i)=>t+i.quantity,0);document.querySelectorAll('[data-cart-count]').forEach(e=>e.textContent=count)}
function bindImageFallbacks(){document.querySelectorAll('img').forEach(img=>{if(img.dataset.fallbackBound)return;img.dataset.fallbackBound='1';img.addEventListener('error',()=>{img.classList.add('image-load-error');const art=img.closest('.product-art,.orbit-art,.studio-mark');if(art&&img.dataset.managedImage==='true'){art.classList.remove('has-image');if(art.classList.contains('orbit-art'))art.style.backgroundImage='none';img.hidden=true;img.removeAttribute('src');img.classList.remove('image-ready');}},{once:true})})}
function addToCart(p){const c=getCart(),e=c.find(i=>i.name===p.name);if(e)e.quantity=Math.min(99,normalizeQuantity(e.quantity)+1);else c.push({name:p.name,price:p.price,image:safeImageUrl(p.image),quantity:1});saveCart(c);updateBagCount();const s=document.querySelector('[data-cart-status]');if(s)s.textContent=`${p.name} added to cart.`}
function navLabel(k){return getContent().siteText?.nav?.[k]||defaultContent.siteText.nav[k]}
function renderHeaderText(){const c=getContent();const t=c.siteText||defaultContent.siteText;document.querySelectorAll('[data-nav-label]').forEach(el=>{const key=el.dataset.navLabel;el.textContent=t.nav?.[key]||defaultContent.siteText.nav[key]||key});const shopCount=String((c.shop||[]).length).padStart(2,'0');document.querySelectorAll('[data-shop-count]').forEach(el=>el.textContent=shopCount);const bag=document.querySelector('.bag-link');if(bag){const isCart=location.pathname.toLowerCase().endsWith('cart.html');const label=bag.querySelector('[data-nav-label]');if(label)label.textContent=isCart?t.nav.cart:t.nav.bag;bag.setAttribute('aria-label',isCart?t.nav.cart:t.nav.bag)}}
function renderPublicContent(){const c=getContent(), text=c.siteText||defaultContent.siteText;
  renderHeaderText();
  document.querySelectorAll('.skip-link').forEach(e=>e.textContent=text.skipToContent);
  document.querySelectorAll('.market-ticker').forEach(e=>e.setAttribute('aria-label',text.liveUpdates));
  const mainPage = document.querySelector('main');
  const pageKey = mainPage?.classList.contains('shop-page')?'shop':mainPage?.classList.contains('gallery-page')?'gallery':mainPage?.classList.contains('live-page')?'live':mainPage?.classList.contains('contact-page')?'contact':mainPage?.classList.contains('about-page')?'us':mainPage?.classList.contains('cart-page')?'cart':null;
  if(pageKey){const p=c.pages[pageKey]||{};const heading=mainPage.querySelector('.page-heading');if(heading){const e=heading.querySelector('.eyebrow'),h=heading.querySelector('h1'),n=heading.querySelector('.heading-note');if(e){let ey=p.eyebrow||'';if(pageKey==='contact'&&c.contact.email)ey=ey.replace(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/,c.contact.email);e.textContent=ey;}if(h)h.innerHTML=`${escapeHtml(p.titleLineOne||'')}<br><em>${escapeHtml(p.titleLineTwo||'')}</em>`;if(n)n.textContent=p.note||'';}}
  const h=c.home||{};document.querySelectorAll('[data-home-eyebrow]').forEach(e=>e.textContent=h.eyebrow);document.querySelectorAll('[data-home-title-one]').forEach(e=>e.textContent=h.titleLineOne);document.querySelectorAll('[data-home-title-two]').forEach(e=>e.textContent=h.titleLineTwo);document.querySelectorAll('[data-home-intro]').forEach(e=>e.textContent=h.intro);document.querySelectorAll('[data-home-cta]').forEach(e=>{e.childNodes[0].textContent=h.cta+' '});document.querySelectorAll('.orbit-art').forEach(art=>{const u=safeImageUrl(h.heroImage);const img=art.querySelector('[data-home-hero-image]');const reset=()=>{art.classList.remove('has-image');art.style.backgroundImage='none';if(img){img.hidden=true;img.removeAttribute('src');img.classList.remove('image-ready');}};if(!img){art.classList.remove('has-image');art.style.backgroundImage='none';return;}reset();if(!u){return;}img.alt=h.heroAlt||'';img.onload=()=>{art.classList.add('has-image');img.hidden=false;img.classList.add('image-ready');};img.onerror=()=>{reset();};img.src=u;});document.querySelectorAll('.label-top').forEach(e=>e.textContent=h.orbitLabelTop||'');document.querySelectorAll('.label-bottom').forEach(e=>e.textContent=h.orbitLabelBottom||'');document.querySelectorAll('.hero-index').forEach(e=>{e.innerHTML=escapeHtml(h.heroIndex||'01 / 04').replace(' / ',' <span>/</span> ')});document.querySelectorAll('.home-hero .section-number,.home-intro .section-number').forEach(e=>e.textContent=h.sectionNumber||'[ 001 ]');document.querySelectorAll('.signal-strip > p:first-child').forEach(e=>e.textContent=h.signalStripLabel||text.home.currentlyTransmitting);document.querySelectorAll('[data-home-signal]').forEach(e=>e.textContent=h.signalText);document.querySelectorAll('[data-home-intro-eyebrow]').forEach(e=>e.textContent=h.introEyebrow);document.querySelectorAll('[data-home-intro-title-one]').forEach(e=>e.textContent=h.introTitleLineOne);document.querySelectorAll('[data-home-intro-title-two]').forEach(e=>e.textContent=h.introTitleLineTwo);document.querySelectorAll('[data-home-intro-description]').forEach(e=>e.textContent=h.introDescription);
  document.querySelectorAll('[data-ticker]').forEach(t=>{t.innerHTML=`<span>${escapeHtml(c.ticker.text)}</span><b>✳</b><span>${escapeHtml(c.ticker.text)}</span><b>✳</b>`;t.style.setProperty('--ticker-speed',`${Math.max(8,Number(c.ticker.speed)||28)}s`);t.style.setProperty('--ticker-font-size',`${Math.max(8,Number(c.ticker.fontSize)||11)}px`)});
  document.querySelectorAll('[data-contact-description], .contact-details .body-copy').forEach(e=>e.textContent=c.contact.description);document.querySelectorAll('[data-contact-email], .contact-links a:first-child').forEach(e=>{e.textContent=`${c.contact.email} ↗`;e.href=`mailto:${c.contact.email}`});document.querySelectorAll('[data-contact-instagram], .contact-links a:last-child').forEach(e=>{e.textContent=`${c.contact.instagram} ↗`;const u=safeExternalUrl(c.contact.instagramUrl);if(u){e.href=u;e.target='_blank';e.rel='noopener noreferrer'}else e.removeAttribute('href')});document.querySelectorAll('[data-contact-section-number]').forEach(e=>e.textContent=c.contact.sectionNumber||'[ 003 ]');document.querySelectorAll('.contact-page .section-number').forEach(e=>e.textContent=c.contact.sectionNumber||'[ 003 ]');document.querySelectorAll('[data-contact-service-title-one]').forEach(e=>e.textContent=c.contact.serviceTitleOne||'We create');document.querySelectorAll('[data-contact-service-title-two]').forEach(e=>e.textContent=c.contact.serviceTitleTwo||'signals.');document.querySelectorAll('[data-contact-form-intro]').forEach(e=>e.textContent=c.contact.formIntro||'');
  document.querySelectorAll('[data-contact-label]').forEach(e=>{const k=e.dataset.contactLabel;e.textContent=text.contact[k]||e.textContent});document.querySelectorAll('[data-contact-submit]').forEach(e=>{e.childNodes[0].textContent=(text.contact.submit||'Transmit')+' '});
  const events=document.querySelector('[data-live-events]')||document.querySelector('.live-page .events');if(events){events.innerHTML=(c.live||[]).map(ev=>{const past=Boolean(ev.past)||(ev.isoDate&&new Date(`${ev.isoDate}T23:59:59`)<new Date()),u=safeExternalUrl(ev.ticketUrl),href=u?escapeHtml(u):'#',target=u?' target="_blank" rel="noopener noreferrer"':'',disabled=u?'':' aria-disabled="true" data-event-link data-no-ticket="true"';return `<a class="event${past?' event-past':''}" href="${href}"${target}${disabled}><time datetime="${escapeHtml(ev.isoDate||'')}">${escapeHtml(ev.date||'')}</time><span>${escapeHtml(ev.location||'')}<small>${escapeHtml(ev.venue||'')}</small></span><span>${escapeHtml(ev.signal||'')}<small>${escapeHtml(ev.detail||'')}</small></span><strong>${escapeHtml(ev.action||text.live.noTicket||'Tickets')} ↗</strong></a>`}).join('');}
  document.querySelectorAll('.event-head span').forEach((e,i)=>{e.textContent=[text.live.date,text.live.location,text.live.signal,''][i]??e.textContent});
  const products=document.querySelector('[data-shop-products]');if(products){products.innerHTML=(c.shop||[]).map((p,i)=>{const u=safeImageUrl(p.image);const artClass=`product-art art-${['moon','signal','coral','poster'][i%4]}${u?' has-image':''}`;const image=u?`<img loading="lazy" decoding="async" draggable="true" data-gallery-image="${escapeHtml(p.name)}" data-managed-image="true" src="${escapeHtml(u)}" alt="${escapeHtml(p.alt||p.name)}">`:'';return `<article class="product product-${String.fromCharCode(97+i)}"><div class="${artClass}">${image}<span>${String(i+1).padStart(2,'0')}</span></div><div class="product-meta"><h2>${escapeHtml(p.name)}</h2><p>${escapeHtml(p.meta||'')}</p><p class="product-description">${escapeHtml(p.description||'')}</p><strong>${escapeHtml(p.price||'€ 0')}</strong><button class="button button-light add-to-cart" data-add-to-cart="${escapeHtml(p.name)}" type="button">${escapeHtml(text.shop.addToCart)} <span>+</span></button></div></article>`}).join('');}
  document.querySelectorAll('.about-page .about-layout .section-number').forEach(e=>e.textContent=c.us.sectionNumber||'[ 002 ]');document.querySelectorAll('[data-us-statement], .about-statement > p:first-child').forEach(e=>e.textContent=c.us.statement);document.querySelectorAll('[data-us-description], .about-statement .body-copy').forEach(e=>e.textContent=c.us.description);const studio=document.querySelector('.studio-mark');if(studio){const u=safeImageUrl(c.us.photo);studio.classList.toggle('has-image',Boolean(u));studio.style.backgroundImage='none';const photo=studio.querySelector('[data-us-photo]');if(photo){if(u){photo.src=u;photo.hidden=false}else{photo.removeAttribute('src');photo.hidden=true}photo.alt=c.us.photoAlt||'';}const label=studio.querySelector('span');if(label)label.textContent=c.us.studioLabel||'KC';}document.querySelectorAll('[data-us-sound-eyebrow]').forEach(e=>e.textContent=c.us.soundEyebrow);document.querySelectorAll('[data-us-sound-title-one]').forEach(e=>e.textContent=c.us.soundTitleLineOne);document.querySelectorAll('[data-us-sound-title-two]').forEach(e=>e.textContent=c.us.soundTitleLineTwo);document.querySelectorAll('[data-us-sound-description]').forEach(e=>e.textContent=c.us.soundDescription);document.querySelectorAll('[data-us-sound-image]').forEach((img,i)=>{const item=c.us.soundImages?.[i]||{};const u=safeImageUrl(item.image);if(u){img.src=u;img.hidden=false}else{img.removeAttribute('src');img.hidden=true}img.alt=item.alt||'';});document.querySelectorAll('[data-us-sound-caption]').forEach((e,i)=>e.textContent=c.us.soundImages?.[i]?.caption||e.textContent);document.querySelectorAll('[data-us-values-eyebrow]').forEach(e=>e.textContent=c.us.valuesEyebrow);document.querySelectorAll('[data-us-values-title-one]').forEach(e=>e.textContent=c.us.valuesTitleLineOne);document.querySelectorAll('[data-us-values-title-two]').forEach(e=>e.textContent=c.us.valuesTitleLineTwo);document.querySelectorAll('[data-us-values-title-three]').forEach(e=>e.textContent=c.us.valuesTitleLineThree);document.querySelectorAll('[data-us-values-description]').forEach(e=>e.textContent=c.us.valuesDescription);
  const shopCount=document.querySelectorAll('[data-shop-count]');shopCount.forEach(e=>e.textContent=String((c.shop||[]).length).padStart(2,'0'));const liveBg=safeImageUrl(c.visuals?.liveBackgroundImage);if(liveBg)document.documentElement.style.setProperty('--live-background-image',`url(${JSON.stringify(liveBg)})`);
  document.querySelectorAll('[data-footer-tagline]').forEach(e=>e.textContent=text.footer.tagline);const path=(location.pathname||'').toLowerCase();let footerKey=path.endsWith('shop.html')?'shopCta':path.endsWith('gallery.html')?'galleryCta':path.endsWith('live.html')?'liveCta':path.endsWith('contact.html')?'contactCta':path.endsWith('cart.html')?'cartCta':path.endsWith('us.html')?'usCta':'homeCta';document.querySelectorAll('.site-footer a').forEach(e=>e.childNodes[0].textContent=(text.footer[footerKey]||e.textContent.replace(' ↗',''))+' ');
}
function setupInteractions(){document.querySelectorAll('[data-add-to-cart]').forEach(b=>b.addEventListener('click',()=>{const p=getContent().shop.find(i=>i.name===b.dataset.addToCart);if(p)addToCart(p)}));document.querySelectorAll('[data-event-link][data-no-ticket]').forEach(l=>l.addEventListener('click',e=>e.preventDefault()))}

async function fetchGalleryAlbums(){
  const r=await fetch(`${API_BASE}/gallery/albums`,{headers:{Accept:'application/json'},cache:'no-store'});
  const d=await r.json().catch(()=>({}));
  if(!r.ok)throw new Error(d.error||'Gallery archive unavailable');
  return Array.isArray(d.albums)?d.albums:[];
}
async function fetchGalleryAlbum(id){
  const r=await fetch(`${API_BASE}/gallery/albums/${encodeURIComponent(id)}`,{headers:{Accept:'application/json'},cache:'no-store'});
  const d=await r.json().catch(()=>({}));
  if(!r.ok)throw new Error(d.error||'Archive folder unavailable');
  return d.album||null;
}
function archiveCard(album){
  const cover=safeImageUrl(album.coverImage||'');
  const mediaCount=Number(album.mediaCount)||0;
  return `<button class="archive-folder" type="button" data-gallery-album="${escapeHtml(album.id)}" aria-label="Open ${escapeHtml(album.title||'archive folder')}">
    <span class="archive-folder-icon" aria-hidden="true"><i></i></span>
    <span class="archive-folder-cover">${cover?`<img loading="lazy" decoding="async" src="${escapeHtml(cover)}" alt="">`:'<span class="archive-folder-placeholder">KC</span>'}</span>
    <span class="archive-folder-copy"><strong>${escapeHtml(album.title||'Untitled night')}</strong><small>${escapeHtml(album.date||'')}${album.location?' / '+escapeHtml(album.location):''}</small><small>${escapeHtml(album.venue||'')} / ${mediaCount} ${mediaCount===1?'item':'items'}</small></span>
  </button>`;
}
function archiveMediaCard(media){
  const url=safeExternalUrl(media.mediaUrl)||safeImageUrl(media.mediaUrl);
  const download=safeExternalUrl(media.downloadUrl)||safeImageUrl(media.downloadUrl);
  const isVideo=String(media.mediaType||'').startsWith('video/');
  const title=media.originalName||`Media ${media.id}`;
  const visual=isVideo?`<video controls preload="metadata" playsinline src="${escapeHtml(url)}"></video>`:`<img loading="lazy" decoding="async" src="${escapeHtml(url)}" alt="${escapeHtml(title)}">`;
  return `<article class="archive-media-card"><div class="archive-media-visual">${visual}</div><div class="archive-media-meta"><strong>${escapeHtml(title)}</strong><small>${escapeHtml(String(media.mediaType||'').replace(/^.*\//,''))} / ${escapeHtml(formatBytes(media.sizeBytes))}</small><a class="archive-download" href="${escapeHtml(download)}">Download ↧</a></div></article>`;
}
function formatBytes(value){const n=Number(value)||0;if(n<1024)return `${n} B`;if(n<1024*1024)return `${(n/1024).toFixed(0)} KB`;if(n<1024*1024*1024)return `${(n/1024/1024).toFixed(1)} MB`;return `${(n/1024/1024/1024).toFixed(1)} GB`;}
async function renderGalleryArchive(){
  const root=document.querySelector('[data-gallery-archive]');
  if(!root)return;
  const hash=(location.hash||'').replace(/^#album=/,'').trim();
  try{
    if(hash){
      const album=await fetchGalleryAlbum(hash);
      if(!album){root.innerHTML='<p class="archive-empty">Archive folder not found.</p>';return;}
      root.innerHTML=`<div class="archive-toolbar"><button class="archive-back" type="button" data-gallery-back>← Back to nights</button><div><strong>${escapeHtml(album.title||'Untitled night')}</strong><span>${escapeHtml(album.date||'')}${album.location?' / '+escapeHtml(album.location):''}${album.venue?' / '+escapeHtml(album.venue):''}</span></div></div><div class="archive-description">${escapeHtml(album.description||'')}</div><div class="archive-media-grid">${(album.media||[]).map(archiveMediaCard).join('')||'<p class="archive-empty">No media has been added to this night yet.</p>'}</div>`;
      root.querySelector('[data-gallery-back]').addEventListener('click',()=>{history.pushState({},'',location.pathname+location.search);renderGalleryArchive();});
      return;
    }
    const albums=await fetchGalleryAlbums();
    root.innerHTML=albums.length?`<div class="archive-grid">${albums.map(archiveCard).join('')}</div>`:'<p class="archive-empty">No night archives yet. The first transmission is waiting.</p>';
    root.querySelectorAll('[data-gallery-album]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.galleryAlbum;history.pushState({},'',`gallery.html#album=${encodeURIComponent(id)}`);renderGalleryArchive();}));
  }catch(err){root.innerHTML=`<p class="archive-empty">${escapeHtml(err.message||'Unable to load the archive.')}</p>`;}
}
function renderCartPage(){const root=document.querySelector('[data-cart-content]');if(!root)return;const c=getContent(),t=c.siteText.cart,cart=getCart();if(!cart.length){root.innerHTML=`<p class="cart-empty">${escapeHtml(t.empty)}</p>`;return;}const total=cart.reduce((s,i)=>s+parsePrice(i.price)*normalizeQuantity(i.quantity),0);root.innerHTML=`<div class="cart-items">${cart.map((i,n)=>`<article class="cart-item"><img loading="lazy" decoding="async" src="${escapeHtml(safeImageUrl(i.image))}" alt="${escapeHtml(i.name)}"><div><h2>${escapeHtml(i.name)}</h2><p>${escapeHtml(i.price)} / ${escapeHtml(t.quantity||'quantity')} ${i.quantity}</p></div><div class="cart-controls"><button type="button" data-cart-action="decrease" data-cart-index="${n}" aria-label="Decrease quantity">−</button><button type="button" data-cart-action="increase" data-cart-index="${n}" aria-label="Increase quantity">+</button><button type="button" data-cart-action="remove" data-cart-index="${n}">${escapeHtml(t.remove)}</button></div></article>`).join('')}</div><div class="cart-total">${escapeHtml(t.total||'Total')} <strong>€ ${total.toFixed(2)}</strong></div><form class="order-form" data-order-form><label>${escapeHtml(t.yourName)}<input type="text" name="name" maxlength="160" autocomplete="name"></label><label>${escapeHtml(t.yourEmail)}<input required type="email" name="email" maxlength="200" autocomplete="email" placeholder="you@example.com"></label><label>${escapeHtml(t.phone)}<input type="tel" name="phone" maxlength="60" autocomplete="tel"></label><label>${escapeHtml(t.address)}<input type="text" name="address" maxlength="250" autocomplete="street-address"></label><label>${escapeHtml(t.city)}<input type="text" name="city" maxlength="120" autocomplete="address-level2"></label><label>${escapeHtml(t.postcode)}<input type="text" name="postcode" maxlength="20" autocomplete="postal-code"></label><label>${escapeHtml(t.country)}<input type="text" name="country" maxlength="80" autocomplete="country-name"></label><button class="button button-light" type="submit">${escapeHtml(t.sendOrder)} <span>↗</span></button></form><p class="cart-status" data-cart-status aria-live="polite"></p>`;root.querySelectorAll('[data-cart-action]').forEach(b=>b.addEventListener('click',()=>{const c2=getCart(),idx=Number(b.dataset.cartIndex),item=c2[idx];if(!item)return;if(b.dataset.cartAction==='increase')item.quantity=Math.min(99,normalizeQuantity(item.quantity)+1);if(b.dataset.cartAction==='decrease')item.quantity=normalizeQuantity(item.quantity)-1;if(b.dataset.cartAction==='remove'||item.quantity<1)c2.splice(idx,1);saveCart(c2);updateBagCount();renderCartPage()}));root.querySelector('[data-order-form]').addEventListener('submit',async e=>{e.preventDefault();const status=root.querySelector('[data-cart-status]');status.textContent=t.sending;const fd=new FormData(e.currentTarget);try{const r=await fetch(`${API_BASE}/checkout`,{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({email:String(fd.get('email')||'').trim(),customer:{name:fd.get('name'),phone:fd.get('phone'),address:fd.get('address'),city:fd.get('city'),postcode:fd.get('postcode'),country:fd.get('country')},items:getCart()})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Checkout failed');if(d.checkoutUrl){window.location.href=d.checkoutUrl;return;}status.textContent=d.message||`Order ${d.orderId} created. Payment setup is not active yet.`}catch(err){status.textContent=err.message||'Unable to send order.'}})}
async function renderPaymentStatus(){const payment=new URLSearchParams(location.search).get('payment'),order=new URLSearchParams(location.search).get('order');if(!payment||!document.querySelector('.cart-page'))return;const box=document.querySelector('[data-cart-content]');if(!box)return;const t=getContent().siteText.cart;if(payment==='cancelled'){const note=document.createElement('p');note.className='cart-status';note.textContent='Payment cancelled. Your cart is still available.';box.prepend(note);return}if(payment==='success'&&order){try{const r=await fetch(`${API_BASE}/orders/status?id=${encodeURIComponent(order)}`,{headers:{Accept:'application/json'},cache:'no-store'});const d=await r.json();const p=document.createElement('p');p.className='cart-status';p.textContent=r.ok?(d.paymentStatus==='PAID'?'Payment received. Your order is confirmed.':'Payment is still being confirmed by the payment provider.'):'Order status could not be confirmed yet.';box.prepend(p)}catch{const p=document.createElement('p');p.className='cart-status';p.textContent='Order status could not be confirmed yet.';box.prepend(p)}}}
function initMatrix(){if(document.querySelector('#matrix-background')||window.matchMedia?.('(prefers-reduced-motion: reduce)').matches)return;const c=document.createElement('canvas');c.id='matrix-background';document.body.prepend(c);const x=c.getContext('2d'),glyphs='KOSMIKCIRCLES01アイウエオカキクケコ';let drops=[];let raf=0;function resize(){c.width=innerWidth;c.height=innerHeight;const cols=Math.min(120,Math.ceil(c.width/20));drops=Array.from({length:cols},()=>Math.random()*-40)}let last=0;function draw(ts=0){const narrow=innerWidth<800;if(narrow&&ts-last<33){raf=requestAnimationFrame(draw);return}last=ts;const speed=Math.max(.05,Number(getContent().matrix?.speed)||.45);x.fillStyle='rgba(9,9,9,.12)';x.fillRect(0,0,c.width,c.height);x.font='14px DM Mono, monospace';drops.forEach((drop,i)=>{x.fillStyle=i%7===0?'rgba(255,90,31,.72)':'rgba(93,255,142,.42)';x.fillText(glyphs[Math.floor(Math.random()*glyphs.length)],i*20,drop*20);drops[i]=drop*20>c.height&&Math.random()>.975?0:drop+speed});raf=requestAnimationFrame(draw)}resize();addEventListener('resize',resize,{passive:true});raf=requestAnimationFrame(draw)}
document.addEventListener('DOMContentLoaded',async()=>{initMatrix();updateCopyrightYear();await fetchContent();renderPublicContent();bindImageFallbacks();setupInteractions();renderCartPage();updateBagCount();await renderPaymentStatus();await renderGalleryArchive();const form=document.querySelector('.contact-form');if(form)form.addEventListener('submit',async e=>{e.preventDefault();const d=new FormData(form);if(String(d.get('website')||'').trim())return;const name=String(d.get('name')||'').trim().slice(0,120),email=String(d.get('email')||'').trim().slice(0,200),message=String(d.get('message')||'').trim().slice(0,4000),status=document.createElement('p');status.className='contact-status';status.setAttribute('role','status');if(!name||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||!message)return;try{const r=await fetch(`${API_BASE}/messages`,{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify({name,email,message,website:d.get('website')})});const result=await r.json();if(!r.ok)throw new Error(result.error||'Unable to send');form.reset();status.textContent='Signal received.'}catch{status.textContent='Signal could not be sent. Please try again.'}form.append(status)})});
window.KosmikCMS={STORAGE_KEY,CART_KEY,defaultContent,getContent,fetchContent,renderPublicContent};

window.addEventListener('popstate',()=>renderGalleryArchive());
window.addEventListener('hashchange',()=>renderGalleryArchive());
